# RF-G-01 — écrans Conseil (candidats, séance, préconisations, seuils) (`src/api/council.js`)
**Quoi** : actes traduits vers `early_warning` — `create_cps_session` (cycle serveur create→convoke→hold), `update_cps_examined(name, examined[])` (LOT), `add_preconisation` (kind ∈ Select serveur), `record_contract_finding` (finding au PLURIEL : « Atteints/Partiellement atteints/Non atteints »), `hold_cps_session` (arbitrage A2 : TENIR est l'acte terminal, pas de « clôturée » distinct). `pronounce_absence_warning` = `add_preconisation(kind="Avertissement d'absences")` avec adaptateur `term→academic_term` (l'audit a attrapé le paramètre qui serait parti dans le vide). **`retain_for_council` est RETIRÉ** (arbitrage A1 : la rétention = inclusion au create, préremplissage calculé) — le bandeau de CouncilCandidatesView:211 qui le nomme est à réécrire au câblage (RF-G-02) en gardant distincts candidats PROPOSÉS / cas RETENUS (règle 1).
**Vocabulaire** : ta table PRECONISATION_TYPES portait DÉJÀ les libellés serveur exacts (clé↔label) — au câblage, envoyer le LABEL. Les verdicts fixtures passés au pluriel serveur. ⚠️ Garde `add_preconisation` = **DE**, pas EM.
**Question MOA en cours** : l'avertissement « subsiste et se retire par acte motivé » — non tranché, aucun retrait n'existe serveur ; ne rien affirmer à l'écran (RF-G-02 retire la phrase).
