# RF-G-01 — écran Administration des rôles (`src/api/roles.js`)
**Quoi** : la surface T5 EXISTE (`role_administration`, garde SM). Traductions : get_role_grant→`get_user_roles_detail(target_user)` · list_assignable_roles→`list_roles_catalog` (l'interdit rendu AVEC sa raison ✓ ton principe) · preview_grant_effect→`simulate_assignment(target_user, kind, value)` — **UN changement par appel** (arbitrage A5 : l'écran enchaîne N simulations, agrège les avertissements W-*, n'écrit qu'avec TOUS les accusés) · assign_role_profile→`apply_profile` · set/clear_role_scope→`add/remove_scope` (motif obligatoire au retrait). `person`→`target_user` partout. Cliqué : tout 200, journaux RAL-2026-00002→5, accusés stockés — l'accusé est bien une DONNÉE, comme tu l'exigeais.
**Codes W serveur** : W-CLOISONNEMENT, W-MAKER-CHECKER, W-PORTEE-NULLE, W-XAPP (tes fixtures utilisent d'autres clés — traduire au câblage).
